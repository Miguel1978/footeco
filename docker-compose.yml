version: '3.8'

services:
  footeco-app:
    build: .
    container_name: footeco-matchsheet
    restart: unless-stopped
    ports:
      - "8085:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - GEMINI_API_KEY=${GEMINI_API_KEY}

